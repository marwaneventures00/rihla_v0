/**
 * Floating “Meet & Greet” chat was removed. Mentor lives only at `/learn/path`.
 * Stub retained so imports do not break; `mentor-events.ts` is unused for now.
 */
export default function CarivaChatBot(_props?: { dockAboveMobileNav?: boolean; hideFab?: boolean }) {
  return null;
}
